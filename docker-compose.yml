version: '3.8'

services:
  app:
    build: .
    container_name: zeus-mcp-diagnosis-container
    ports:
      - "6000:6000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
